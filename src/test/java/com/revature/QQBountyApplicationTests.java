package com.revature;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.revature.models.Bounty;

@RunWith(SpringRunner.class)
@SpringBootTest
public class QQBountyApplicationTests {

	//Test is currently broken with failed to load application context error
//	@Test 
//	public void bountyTest()  {
//	        //  create mock
//	        Bounty test = mock(Bounty.class);
// 	        // define return value for method getUniqueId()
//	        when(test.getBountyId()).thenReturn(42);
// 	        // use mock in test....
//	        assertEquals(test.getBountyId(), 42);
//		}
	
	
	}
	
